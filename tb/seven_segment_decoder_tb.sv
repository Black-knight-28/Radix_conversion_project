`timescale 1ns / 1ps

module seven_segment_decoder_tb;

    // Inputs to DUT
    logic [3:0] digit;

    // Output from DUT
    logic [6:0] seg;


    // Instantiate the decoder
    seven_segment_decoder uut (
        .digit(digit),
        .seg(seg)
    );


    initial begin

        // ==========================================
        // Test 0
        // ==========================================

        digit = 4'd0;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 1
        // ==========================================

        digit = 4'd1;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 2
        // ==========================================

        digit = 4'd2;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 3
        // ==========================================

        digit = 4'd3;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 4
        // ==========================================

        digit = 4'd4;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 5
        // ==========================================

        digit = 4'd5;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 6
        // ==========================================

        digit = 4'd6;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 7
        // ==========================================

        digit = 4'd7;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 8
        // ==========================================

        digit = 4'd8;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test 9
        // ==========================================

        digit = 4'd9;
        #10;
        $display("Digit = %0d | Seg = %7b", digit, seg);


        // ==========================================
        // Test A
        // ==========================================

        digit = 4'd10;
        #10;
        $display("Digit = A | Seg = %7b", seg);


        // ==========================================
        // Test B
        // ==========================================

        digit = 4'd11;
        #10;
        $display("Digit = B | Seg = %7b", seg);


        // ==========================================
        // Test C
        // ==========================================

        digit = 4'd12;
        #10;
        $display("Digit = C | Seg = %7b", seg);


        // ==========================================
        // Test D
        // ==========================================

        digit = 4'd13;
        #10;
        $display("Digit = D | Seg = %7b", seg);


        // ==========================================
        // Test E
        // ==========================================

        digit = 4'd14;
        #10;
        $display("Digit = E | Seg = %7b", seg);


        // ==========================================
        // Finish simulation
        // ==========================================

        $finish;

    end

endmodule